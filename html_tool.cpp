#include "html_tool.h"

String HtmlTool::ReadHtmlFile(char* FilePath)
{
    // char buf[MAX_LENTH] = {0};
    // char ReturnString[MAX_LENTH] = {0};
    // File* HtmlFile;
    // int FileLenth = 0;
    // if ((HtmlFile = SPIFFS.open("/index.html", "r");) == NULL)
    // {
    //     Serial.print("HtmlFile:");
    //     Serial.print(FilePath);
    //     Serial.println("fail to read");
    //     return "E";
    // }
    // while (fgets(buf,MAX_LENTH,HtmlFile))
    // {
    //     FileLenth = strlen(buf);
    //     buf[FileLenth-1] = '\0';
    //     strcpy(ReturnString,buf);
    // }
    // return String(ReturnString);
}